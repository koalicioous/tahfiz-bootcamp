import { View, Text } from "../components/Themed";
import { RootStackScreenProps } from "../types";

export default function Surah({ navigation }: RootStackScreenProps<"Surah">) {
  return (
    <View>
      <Text>Surah</Text>
    </View>
  );
}
